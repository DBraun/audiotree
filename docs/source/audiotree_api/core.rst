.. role:: python(code)
     :language: python
     :class: highlight

audiotree
===========================

..

.. ---------------------------

.. Documented at ``audiotree.AudioTree`` rather than the ``audiotree.core``
   submodule it is defined in, so the public spelling is the one guides and
   cross-references resolve against -- ``audiotree.core`` is an internal path
   that may move, so a ``:class:`audiotree.AudioTree``` in a user-facing page
   should not point at it.
   Same reasoning as ``audiotree.sources.TreeDataSource`` in ``tree.rst``.

.. autoclass:: audiotree.AudioTree
   :members:
   :undoc-members:

.. autoclass:: audiotree.ExcerptConfig
   :members:
   :undoc-members:
