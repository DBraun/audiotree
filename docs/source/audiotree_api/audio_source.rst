.. role:: python(code)
     :language: python
     :class: highlight

AudioDataSource
===========================

..

.. ---------------------------

.. Documented under the ``audiotree.sources`` package rather than the
   ``audiotree.sources.audio`` submodule it lives in, so that the public
   spelling is the one cross-references resolve against. ``autoclass`` rather
   than ``automodule`` so the package itself is described only once, on the
   :doc:`sources` page.

.. autoclass:: audiotree.sources.AudioDataSource
   :members:
   :undoc-members:
